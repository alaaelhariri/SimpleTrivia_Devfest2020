import { SecretDirective } from './secret.directive';

describe('SecretDirective', () => {
  it('should create an instance', () => {
    const directive = new SecretDirective();
    expect(directive).toBeTruthy();
  });
});
